pipeline {
    agent any
    stages {
        stage('build') {
            steps {
//                echo "Integrating My First Jenkins Pipeline with GitHub WebHook Using Jenkinsfile"
                sh 'python --version'

                sh 'python hello-world.py'
            }
        }
    }
}