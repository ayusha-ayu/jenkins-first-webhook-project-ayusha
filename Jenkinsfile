pipeline{
    agent any
    stages{
        stage('build'){
            steps{
                echo 'ayushas first jenkinsfile'
                sh 'python --version'
                sh 'python hello-world.py'
            }
        }
    }
}